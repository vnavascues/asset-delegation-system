```
DeployDelegateRegistryScript::run
# when DelegateRegistry does not exist
## it should deploy
## it should return address
# when DelegateRegistry exists
## it should revert
```

```
DeployDelegateRegistryScript::run
├── when DelegateRegistry does not exist
│   ├── it should deploy
│   └── it should return address
└── when DelegateRegistry exists
    └── it should revert
```
